enum AttributeType {
    dynamicAttrubute,
    dynamicStyle,
    dynamicClass,
    staticAttribute,
    style,
    class,
    event,
}
enum DynamicType {
    dynamicAttrubute,
    dynamicStyle,
    dynamicClass,
}
export { AttributeType, DynamicType };
